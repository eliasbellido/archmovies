<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="review"
            type="pe.lumindevs.archmovies.entity.Review" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <TextView
            android:id="@+id/item_review_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{review.author}"
            android:background="@color/background"
            android:textColor="@color/white"
            android:textSize="17sp"
            android:textStyle="bold"
            tools:text="title"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.ms.square.android.expandabletextview.ExpandableTextView
            android:id="@+id/item_review_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:animAlphaStart="1"
            app:animDuration="200"
            app:bindExpandableTextView="@{review.content}"
            app:collapseDrawable="@drawable/ic_arrow_up"
            app:expandDrawable="@drawable/ic_arrow_down"
            app:layout_constraintStart_toStartOf="@+id/item_review_title"
            app:layout_constraintTop_toTopOf="@+id/item_review_title"
            app:maxCollapsedLines="4">

            <TextView
                android:id="@id/expandable_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="7dp"
                android:background="?android:attr/selectableItemBackground"
                android:fontFamily="sans-serif-light"
                android:lineSpacingMultiplier="1.2"
                android:textAppearance="@style/TextAppearance.AppCompat.Body1"
                android:textColor="@color/white"
                android:textSize="14sp"
                tools:text="content" />

            <ImageButton
                android:id="@id/expand_collapse"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end|bottom"
                android:background="?android:attr/selectableItemBackground"
                android:padding="6dp" />


        </com.ms.square.android.expandabletextview.ExpandableTextView>


    </androidx.constraintlayout.widget.ConstraintLayout>





</layout>